[project]
name = "workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.10.2",
    "weasyprint>=69.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "python-pptx>=1.0.2",
    "pyyaml>=6.0.3",
    "ruff>=0.15.17",
]
